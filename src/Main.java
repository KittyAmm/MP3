import dao.BaseModele;
import dao.GenericDao;
import model.Personne;
import model.Produit;

import java.util.ArrayList;

public class Main {
    public static void main(String[] arg) {
      /*  GenericDao gdao= new GenericDao();
        Personne p = new Personne("minou", "malala");
        Produit p1 = new Produit("kapa","23/07/1998",1000);
        //p.setId("2");
        //gdao.save(p1);
       // gdao.save(p);
      // System.out.println(new GenericDao().queryFindAll(p));
        ArrayList<BaseModele> ba = gdao.findAll(p);
        for(int i = 0; i < ba.size(); i++){
            Personne pers = (Personne)ba.get(i);
            System.out.println(String.valueOf(pers));
        }*/
    }
}
